# hooks/

Cross-feature React hooks. Domain hooks wrap Firebase / AI / TTS state
(`useRoom`, `useAIVoice`, `useMCPlayer`, `useAutoRandom`); `useAutoHide` is a
generic UI helper. All client-only (`'use client'`).
