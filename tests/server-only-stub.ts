// Empty stub for the Next.js `server-only` marker package under vitest.
// At runtime in Next, importing 'server-only' from a client module is a
// compile-time error; under vitest we just need the import to resolve.
export {};
