// Stub for the 'server-only' Next.js package. In production Next.js throws
// when this module is imported from the client bundle; in Vitest there is no
// such bundler boundary, so we simply export nothing to let the import pass.
export {};
