# features/

Feature modules. Each subdirectory holds one product surface: the page-level
client component plus any widgets only that feature uses. Cross-feature UI
lives at the top-level `components/`.
