# components/

Components imported by more than one feature: `VideoPlayer`, `EmojiLayer`,
`ConfirmDialog`, `ThemeProvider`. Feature-private widgets belong under
`features/<name>/components/`, not here.
