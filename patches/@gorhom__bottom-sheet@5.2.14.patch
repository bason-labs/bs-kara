diff --git a/lib/commonjs/hooks/useAnimatedLayout.js b/lib/commonjs/hooks/useAnimatedLayout.js
index 973d05c300911867ad6e91df2b4d4aefe5c2771e..794480f2a9bc0cc9069f260d58ca3b973baa534c 100644
--- a/lib/commonjs/hooks/useAnimatedLayout.js
+++ b/lib/commonjs/hooks/useAnimatedLayout.js
@@ -87,12 +87,12 @@ function useAnimatedLayout(containerLayoutState, topInset, bottomInset, modal, s
   }, [state, verticalInset, modal]);
   (0, _react.useEffect)(() => {
     _reactNative.Dimensions.addEventListener('change', ({
-      window
+      window: nextWindow
     }) => {
       state.modify(_state => {
         'worklet';
 
-        _state.window = window;
+        _state.window = nextWindow;
         return _state;
       });
     });
diff --git a/lib/module/hooks/useAnimatedLayout.js b/lib/module/hooks/useAnimatedLayout.js
index b0eec9361af936659c777ea9d967981c03ad92c6..c31dad46ad2e0db400c68e6eff6e62c254abec6c 100644
--- a/lib/module/hooks/useAnimatedLayout.js
+++ b/lib/module/hooks/useAnimatedLayout.js
@@ -83,12 +83,12 @@ export function useAnimatedLayout(containerLayoutState, topInset, bottomInset, m
   }, [state, verticalInset, modal]);
   useEffect(() => {
     Dimensions.addEventListener('change', ({
-      window
+      window: nextWindow
     }) => {
       state.modify(_state => {
         'worklet';
 
-        _state.window = window;
+        _state.window = nextWindow;
         return _state;
       });
     });
diff --git a/src/hooks/useAnimatedLayout.ts b/src/hooks/useAnimatedLayout.ts
index 68c674de6287b89a97be778f7e5273f6b6b1c929..6c01302eff4bf17aaad354e3a4a6aa76bf8ecc71 100644
--- a/src/hooks/useAnimatedLayout.ts
+++ b/src/hooks/useAnimatedLayout.ts
@@ -106,10 +106,13 @@ export function useAnimatedLayout(
     [state, verticalInset, modal]
   );
   useEffect(() => {
-    Dimensions.addEventListener('change', ({ window }) => {
+    // Renamed `window` -> `nextWindow`: Reanimated's worklet plugin treats
+    // `window` as a built-in global and skips closure capture, so the worklet
+    // throws "Property 'window' doesn't exist" on Dimensions change (e.g. rotation).
+    Dimensions.addEventListener('change', ({ window: nextWindow }) => {
       state.modify(_state => {
         'worklet';
-        _state.window = window;
+        _state.window = nextWindow;
         return _state;
       });
     });
