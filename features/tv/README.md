# features/tv/

The shared TV display (Next route `/tv`). `TVClient.tsx` is the route shell.
The TV's queue panel and waiting overlay are inline; cross-feature primitives
(VideoPlayer, EmojiLayer, ConfirmDialog) live in `/components`.
