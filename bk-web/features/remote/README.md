# features/remote/

The phone remote (Next route `/`). `RemoteClient.tsx` is the route shell;
`components/` holds widgets used only by the remote (search, queue, settings,
fullscreen player, etc.). Shared primitives are in `/components`.
